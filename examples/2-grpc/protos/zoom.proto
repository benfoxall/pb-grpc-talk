syntax = "proto3";

message Noop {}

message DarkModeRequest {
    bool on = 1;
};

message BrightnessRequest {
    float value = 1;
}

service Zoom {
    rpc setDarkMode (DarkModeRequest) returns (Noop);

    rpc setBrightness (BrightnessRequest) returns (Noop);
}
